<?
class BusinessAdmLocalidadeDiaria extends Business
{
	public $idLocalidadeDiaria;
	public $idRequisicao;
	public $idVinculo;
	public $idMunicipio;
	public $quantDiarias;

	function __construct($data = null)
	{
		parent::__construct('sigaept', $data);
	}

    function OnCreate($data)
    {
        if (empty($data))
        {
//            $this->initialize();
        }
        else
        {
            $this->GetById($data);
        }
    }

    /*
	function SetData($data)
	{
		foreach($data as $attr=>$value)
		{
			$this->$attr = $value;
		}
	}
    */
    
	function GetByIds($idRequisicao,$idVinculo,$idMunicipio)
	{
		$this->idRequisicao = $idRequisicao;
        $this->idVinculo = $idVinculo;
        $this->idMunicipio = $idMunicipio;
		$this->retrieve();
		return $this;
	}
    
	function GetByIdRequisicao($id)
	{
		$this->idRequisicao = $id;
		$this->retrieve();
		return $this;
	}
}
?>
