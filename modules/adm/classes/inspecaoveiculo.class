<?
class BusinessAdmInspecaoVeiculo extends Business {

		public $idRequisicao;
		public $idComponente;
		public $status;
		public $obs;


		function __construct($data = null) {
				parent::__construct('sigaept', $data);
		}

		function OnCreate($data) {
				if (empty($data)) {
//           $this->initialize();
				} else {
						$this->GetById($data);
				}
		}

		function GetById($idRequisicao, $idComponente) {
				$this->idRequisicao = $idRequisicao;
				$this->idComponente = $idComponente;
				$this->retrieve();

				return $this;
		}

}
