<?
/**
 * Brief Class Description.
 * Complete Class Description.
 * @author UFJF/CGCO
 * @access public
 */
class BusinessAdmFolhaPF extends MBusiness
{
	var $idFolhaPF;
	var $mes;
	var $ano;
	var $dataPag;
	
	/**
     * Constructor.
     * BusinessAdmFolhaPF Class Constructor.
     * @param $data (tipo) desc
     */
	function __construct($data = null)
	{
		parent::__construct('sigaept', $data);
	}
	
	
	/**
     * Método onde se passa um id como parametro e a folha correspondente ao
     * id é retornado para a variável o que chamou.
	 *
	 * @name getById
	 * @param $idFolhaPF (tipo) id da folha. 
	 * @access public
	 */
	function getById($idFolhaPF)
	{
		$this->idFolhaPF = $idFolhaPF;
		$this->retrieve();
		return $this;
	} 
    
	
	function onCreate($data)
    {
        if (empty($data))
        {
		//$this->initialize();
        }
        else
        {
            $this->getById($data);
        }
    }
    
}
?>
