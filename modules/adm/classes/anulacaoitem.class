<?
class BusinessAdmAnulacaoItem extends Business
{
	var $idAnulacaoItem;
	var $idLicitacao;
	var $item;
	var $data;
	var $valorUnitarioAnulado;
	var $quantidadeAnulada;

	function __construct($data = null)
    {
       parent::__construct('sigaept',$data);
    }

    function OnCreate($data)
    {
        if (empty($data))
        {
//            $this->initialize();
        }
        else
        {
            $this->GetById($data);
        }
    }

	function GetById($data)
	{
		$this->idLicitacao = $data[0];
		$this->item = $data[1];
		$this->data = $data[2];
		$this->retrieve();
		return $this;
	}

}
?>
