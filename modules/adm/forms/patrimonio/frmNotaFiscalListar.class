<?php
class frmNotaFiscalListar extends MForm
{


   public $iditem;
   private $grid;




    function __construct()
    {
        global $MIOLO,$module,$action,$item;

	$idNotaFiscal=$_GET['item'];
	$ui = $MIOLO->GetUI();
	$this->grid = $ui->GetGrid('adm','gridPatrimonioNf');

	parent::__construct('Listagem dos Itens Relacionados');
        $this->SetClose(null);
        $this->EventHandler();
	$this->defaultButton=NULL;
    }

    function CreateFields()
    {
        global $MIOLO,$module;

      $fields = array(

			$this->grid
		);

			$this->SetFields($fields);
			$this->page->addJsCode(file_get_contents(dirname(__FILE__) . '/../../ui/js/mask.js'));

    }




}
?>