<?
/**
 * Formulário de exclusão uo
 */
class frmRequisicaoDel extends Form
{
	/**
	 * Objeto uo
	 * @access protected
	 * @var BusinessRhRequisicao
	 */
	protected $objRequisicao;

    function __construct($objRequisicao)
    {
		$this->objRequisicao = $objRequisicao;
        parent::__construct('Excluir Requisição');
        $this->EventHandler();
    }

    function CreateFields()
    {
		$action_sim = $this->manager->GetActionURL('adm','main:requisicoes',$this->objRequisicao->idRequisicao,array('form'=>'excluir','conf'=>'sim'));
		$action_nao = $this->manager->GetActionURL('adm','main:requisicoes',$this->objRequisicao->idRequisicao,array("form"=>"item","tipoReq"=>$this->objRequisicao->idTipoReq));

		$conf = $this->GetFormValue('conf');

		if ($conf == 'sim')
		{
			$this->objRequisicao->Delete();
			if (!$this->objRequisicao->CheckError())
			{
				$this->manager->Information('Requisição excluída com sucesso',$this->manager->GetActionURL('adm','main:requisicoes:find'));
			}
			else
			{
				foreach( $this->objRequisicao->GetErrors() as $e )
				{
					$err .= " $e ";
				}
				$this->manager->Error("Informe o seguinte erro à equipe do CGCO: ".$e,$this->manager->GetActionURL('adm','main:requisicoes',$this->objRequisicao->idRequisicao,array("form"=>"item","tipoReq"=>$this->objRequisicao->idTipoReq)));
			}
					
	   }
	   else
	   {
		   $this->manager->Question("Tem certeza que deseja excluir a requisição ".$this->objRequisicao->idRequisicao,$action_sim,$action_nao);
		}
	   
    }


 }
?>
