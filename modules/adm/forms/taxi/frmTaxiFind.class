<?
global $MIOLO;
/**
 * Use Module Control
 */
$MIOLO->Uses('controls/linktaxi.class','adm');

/**
 * Formulário básico para seleção de Taxi
 * 
 */
class frmTaxiFind extends Form
{
    function __construct()
    {
		global $MIOLO, $module;
		
        $this->Form('Pesquisar Taxi');
		$this->SetClose($MIOLO->GetActionURL($module,'main:mantutencao'));
        $this->EventHandler();
    }

    function CreateFields()
    {
        global $MIOLO, $action;

        $ui = $MIOLO->GetUI();
        $grid = $ui->GetGrid('adm','gridTaxi');
        $grid->SetTitle(NULL);

        
        $lnkNew = new LinkTaxi('lnkNew', FALSE);
        $lnkNew->setNew();
        
        $findButton = new ButtonFind('');
        $findButton->SetName('btnFind');
        
        $fields = array(
            array(
                $lnkNew
            ),
            $grid
        );
        $this->SetFields($fields);
        $this->defaultButton = false;
    }
 }
?>
