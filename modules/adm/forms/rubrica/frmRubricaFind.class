<?
global $MIOLO;
/**
 * Use Module Control
 */
$MIOLO->Uses('controls/linkrubrica.class','adm');

/**
 * Formulário básico para seleção de rubricas
 * 
 */
class frmRubricaFind extends Form
{
    function __construct()
    {
		global $MIOLO, $module;
        $this->Form('Pesquisar Rubricas');
		$this->SetClose($MIOLO->GetActionURL($module,'main:manutencao'));
        $this->EventHandler();
    }

    function CreateFields()
    {
        global $MIOLO, $action;

		$ui = $MIOLO->GetUI();
        $grid = $ui->GetGrid('adm','gridRubrica');
        $grid->SetTitle(NULL);

        
        $lnkNew = new LinkRubrica('lnkNew', FALSE);
        $lnkNew->setNew();
        
        $findButton = new ButtonFind('');
        $findButton->SetName('btnFind');
        
        $fields = array(
            array(
                new Text('','Rubrica:'),
                new TextField('rubrica','','',15),
                $findButton,
                $lnkNew
            ),
            $grid
        );
        $this->SetFields($fields);
        $this->defaultButton = false;
    }
 }
?>
