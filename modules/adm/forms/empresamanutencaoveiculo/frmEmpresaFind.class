<?php
global $MIOLO;
$MIOLO->Uses('controls/linkempresamanutencaoveiculo.class','adm');

class frmEmpresaFind extends MForm
{
    protected $empresa;
    
    function __construct()
    {
        global $MIOLO;
        $this->empresa = $MIOLO->getBusiness('adm','empresamanutencaoveiculo');
        parent::__construct('Empresa');
        $this->SetClose($this->manager->GetActionURL('adm','main'));
        $this->eventHandler();
    }
    function CreateFields()
    {
        $grid = $this->manager->GetUI()->GetGrid('adm','gridEmpresaManutencaoVeiculo');
        $grid->SetTitle(NULL);
        $btnFind = new ButtonFind('btnBusca');
        $busca  = Form::GetFormValue('busca');
        $novo = new LinkEmpresaManutencaoVeiculo('lnkNovo',FALSE);
        $novo->setNew();
        $formLink->width = '30%';
        $formLink->float = 'right';
        $formLink->padding = '6px';
        $formLink->height = '114px';
        $fields = array(
            new Spacer('5px'),
	    $novo,
            $grid,
        );
        $this->SetFields($fields);
        $this->defaultButton = false;
    }
 }
?>