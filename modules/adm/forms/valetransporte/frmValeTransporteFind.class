<?
global $MIOLO;
/**
 * Use Module Control
 */
$MIOLO->Uses('controls/linkvaletransporte.class','adm');

/**
 * Formulário básico para seleção de 
 * 
 */
class frmValeTransporteFind extends Form
{
    function __construct()
    {
		global $MIOLO, $module;
		
        $this->Form('Pesquisar Vales Transporte');
		$this->SetClose($MIOLO->GetActionURL($module,'main:manutencao'));
        $this->EventHandler();
    }

    function CreateFields()
    {
        global $MIOLO, $action;

        $ui = $MIOLO->GetUI();
        $grid = $ui->GetGrid('adm','gridValeTransporte');
        $grid->SetTitle(NULL);

        
        $lnkNew = new LinkValeTransporte('lnkNew', FALSE);
        $lnkNew->setNew();
        
        $findButton = new ButtonFind('');
        $findButton->SetName('btnFind');
        
       $fields = array(
            array(
 //               new Text('','Vale Transporte:'),
 //                new TextField('valetransporte','','',15),
 //               $findButton,
                $lnkNew
            ),
            $grid
        );

        $this->SetFields($fields);

        $this->defaultButton = false;
    }
 }
?>
