<?
global $MIOLO;
/**
 * Use Module Control
 */
$MIOLO->Uses('controls/linkmotorista1.class','adm');

/**
 * Formulário básico para seleção de Hotéis
 * 
 */
class frmMotoristaFind extends Form
{
    function __construct()
    {
		global $MIOLO, $module;
		
        $this->Form('Pesquisar Motoristas');
		$this->SetClose($MIOLO->GetActionURL($module,'main:mantutencao'));
        $this->EventHandler();
    }

    function CreateFields()
    {
        global $MIOLO, $action,$item;

        $ui = $MIOLO->GetUI();
        $grid = $ui->GetGrid('adm','gridMotorista1');
        $grid->SetTitle(NULL);

        $session = $MIOLO->getSession();
        $session->setValue('itemuom', $item);
        $lnkNew = new LinkMotorista1('lnkNew', FALSE);
        $lnkNew->setNew();
        
        $findButton = new ButtonFind('');
        $findButton->SetName('btnFind');
        
        $fields = array(
            array(
                new Text('','Motorista:'),
                new TextField('motorista','','',15),
                $findButton,
                $lnkNew
            ),
            $grid
        );
        $this->SetFields($fields);
        $this->defaultButton = false;
    }
 }
?>
