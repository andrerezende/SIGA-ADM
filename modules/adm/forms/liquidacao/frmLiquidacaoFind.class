<?
global $MIOLO;
/**
 * Use Module Control
 */
$MIOLO->Uses('controls/linkliquidacao.class','adm');

/**
 * Formulário básico para seleção de liquidacaos
 * 
 */
class frmLiquidacaoFind extends Form
{
    function __construct()
    {
		global $MIOLO, $module;
		
        $this->Form('Pesquisar Liquidações');
		$this->SetClose($MIOLO->GetActionURL($module,'main:mantutencao'));
        $this->EventHandler();
    }

    function CreateFields()
    {
        global $MIOLO, $action;

        $ui = $MIOLO->GetUI();
        $grid = $ui->GetGrid('adm','gridLiquidacao');
        $grid->SetTitle(NULL);

        
        $lnkNew = new LinkLiquidacao('lnkNew', FALSE);
        $lnkNew->setNew();
        
        $findButton = new ButtonFind('');
        $findButton->SetName('btnFind');
        
        $fields = array(
            array(
                new Text('','Data Liquidação:'),
                new TextField('liquidacao','','',15),
                $findButton,
                $lnkNew
            ),
            $grid
        );
        $this->SetFields($fields);
        $this->defaultButton = false;
    }
 }
?>
