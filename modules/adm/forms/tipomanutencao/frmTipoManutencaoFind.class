<?php
global $MIOLO;
$MIOLO->Uses('controls/linktipomanutencao.class','adm');

class frmTipoManutencaoFind extends MForm
{
    protected $tipoManutencao;
    
    function __construct()
    {
        global $MIOLO;
        $this->tipoManutencao = $MIOLO->getBusiness('adm','tipomanutencao');
        parent::__construct('Tipos de Manutenção');
        $this->SetClose($this->manager->GetActionURL('manutencaoveiculos','main'));
        $this->eventHandler();
    }
    function CreateFields()
    {
//      grid
        $grid = $this->manager->GetUI()->GetGrid('adm','gridTipoManutencao');
        $grid->SetTitle(NULL);
        $btnFind = new ButtonFind('btnBusca');
        $busca  = Form::GetFormValue('busca');
        $novo = new LinkTipoManutencao('lnkNovo',FALSE);
        $novo->setNew();
        $formLink->width = '30%';
        $formLink->float = 'right';
        $formLink->padding = '6px';
        $formLink->height = '114px';
//      fields
        $fields = array(
            new Spacer('5px'),
	    $novo,
            $grid,
        );
        $this->SetFields($fields);
        $this->defaultButton = false;
    }
 }
?>
