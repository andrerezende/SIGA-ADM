<?php
global $MIOLO;
$MIOLO->Uses('controls/linknotafiscalmanutencaoveiculo.class','adm');

class frmNotaFiscalFind extends MForm
{
    protected $notaFiscal;
    
    function __construct()
    {
        global $MIOLO;
        $this->notaFiscal = $MIOLO->getBusiness('adm','notafiscalmanutencaoveiculo');
        parent::__construct('Nota');
        $this->SetClose($this->manager->GetActionURL('adm','main'));
        $this->eventHandler();
    }
    function CreateFields()
    {
//      grid
        $grid = $this->manager->GetUI()->GetGrid('adm','gridNotaFiscalManutencaoVeiculo');
        $grid->SetTitle(NULL);
        $btnFind = new ButtonFind('btnBusca');
        $busca  = Form::GetFormValue('busca');
        $novo = new LinkNotaFiscalManutencaoVeiculo('lnkNovo',FALSE);
        $novo->setNew();
        $formLink->width = '30%';
        $formLink->float = 'right';
        $formLink->padding = '6px';
        $formLink->height = '114px';
//      fields
        $fields = array(
            new Spacer('5px'),
	    $novo,
            $grid,
        );
        $this->SetFields($fields);
        $this->defaultButton = false;
    }
 }
?>
