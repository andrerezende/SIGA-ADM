<?
global $MIOLO;
/**
 * Use Module Control
 */
$MIOLO->Uses('controls/linktiporeq.class','adm');

/**
 * Formulário básico para seleção de tiporeqs
 * 
 */
class frmTipoReqFind extends Form
{
    function __construct()
    {
		global $MIOLO, $module;
		
        $this->Form('Pesquisar Tipos de Requisição');
		$this->SetClose($MIOLO->GetActionURL($module,'main:mantutencao'));
        $this->EventHandler();
    }

    function CreateFields()
    {
        global $MIOLO, $action;

        $ui = $MIOLO->GetUI();
        $grid = $ui->GetGrid('adm','gridTipoReq');
        $grid->SetTitle(NULL);

        
        $lnkNew = new LinkTipoReq('lnkNew', FALSE);
        $lnkNew->setNew();
        
        $findButton = new ButtonFind('');
        $findButton->SetName('btnFind');
        
        $fields = array(
            array(
                new Text('','Tipo de Requisição:'),
                new TextField('tiporeq','','',15),
                $findButton,
                $lnkNew
            ),
            $grid
        );
        $this->SetFields($fields);
        $this->defaultButton = false;
    }
 }
?>
