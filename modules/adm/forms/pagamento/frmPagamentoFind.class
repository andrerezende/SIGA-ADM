<?
global $MIOLO;
/**
 * Use Module Control
 */
$MIOLO->Uses('controls/linkpagamentoliquidacao.class','adm');

/**
 * Formulário básico para seleção de Pag. liquidacao
 * 
 */
class frmPagamentoFind extends Form
{
    function __construct()
    {
		global $MIOLO, $module;
		
        $this->Form('Pesquisar Liquidações');
		$this->SetClose($MIOLO->GetActionURL($module,'main:mantutencao'));
        $this->EventHandler();
    }

    function CreateFields()
    {
        global $MIOLO, $action;

        $ui = $MIOLO->GetUI();
        $grid = $ui->GetGrid('adm','gridPagamentoLiquidacao');
        $grid->SetTitle(NULL);

        
        $findButton = new ButtonFind('');
        $findButton->SetName('btnFind');
        
        $fields = array(
            array(
                new Text('','Data Liquidação:'),
                new TextField('liquidacao','','',15),
                $findButton
            ),
            $grid
        );
        $this->SetFields($fields);
        $this->defaultButton = false;
    }
 }
?>
