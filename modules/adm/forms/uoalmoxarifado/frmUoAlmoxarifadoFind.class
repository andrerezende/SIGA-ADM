<?
MIOLO::Import('modules::adm::controls::linkuoalmoxarifado');
class frmUoAlmoxarifadoFind extends Form
{
    function frmFuncionarioFind()
    {
        $this->Form('Pesquisar Almoxarifados');
        $this->EventHandler();
    }

    function CreateFields()
    {
        $grid = $this->manager->GetUI()->GetGrid('adm','gridUoAlmoxarifado');
        $grid->SetTitle(NULL);
        $btnFind = new ButtonFind('');
        $newLink = new LinkUoAlmoxarifado('lnkNew',FALSE);
        $fields = array(
            $grid
        );
        $this->SetFields($fields);
        $this->defaultButton = false;
    }
 }
?>
