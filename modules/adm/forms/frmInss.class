<?
global $MIOLO;
/**
 * Use Module Control
 */
//$MIOLO->Uses('controls/linkvaletransporte.class','adm');

/**
 * Formulário básico para seleção de 
 * 
 */
class frmValeTransporteFind extends Form
{
    function __construct()
    {
		global $MIOLO, $module;
		
        $this->Form('INSS');
		$this->SetClose($MIOLO->GetActionURL($module,'main:mantutencao'));
        $this->EventHandler();
    }

    function CreateFields()
    {
        global $MIOLO, $action;

        $ui = $MIOLO->GetUI();
        $grid = $ui->GetGrid('adm','gridInss');
        $grid->SetTitle(NULL);

        
        $lnkNew = new LinkInss('lnkNew', FALSE);
        $lnkNew->setNew();
        
                
       $fields = array(
            array(

                $lnkNew
            ),
            $grid
        );

        $this->SetFields($fields);

        $this->defaultButton = false;
    }
 }
?>
