<?
class frmItemEmpenho extends MForm
{
    function __construct()
    {
		global $MIOLO, $module;
        parent::__construct('Pesquisa de empenhos');
		$this->SetClose($MIOLO->GetActionURL($module,'main'));
        $this->EventHandler();
    }
    function CreateFields()
    {
		$optionsStatus = Array('1'=>'A Numerar','2'=>'Empenhado' );
		$grid = $this->manager->getUI()->getGrid('adm','gridEmpenho2');
		$fields = array($busca,
						$grid
					);
        $this->setFields($fields);
	    $this->defaultButton = false;

    }
 }
?>
