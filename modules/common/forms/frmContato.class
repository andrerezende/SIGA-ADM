<?
class frmContato extends Form
{
	function frmContato()
    {   
        $this->Form('Contato');
	    $this->EventHandler();
	}

    function CreateFields()
	{  
       $fields = Array(
           new Text('','Implementar formulário para contato'),
       );
	   $this->SetFields($fields);
       $buttons = Array(
           new FormButton('btnPost', 'Enviar')
       );
	   $this->SetButtons($buttons);
	}

	function btnPost_click()
	{
    }
}
?>
