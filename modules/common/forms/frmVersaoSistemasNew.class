<?

class frmVersaoSistemasNew extends MForm {


    function __construct() {
        
        $this->getBusiness('protocolo','processo');
        $this->getBusiness('protocolo','versaossitemas');
//        $this->setoracesso = $MIOLO->getBusiness('protocolo','setoracesso');
//        $this->setor = $MIOLO->getBusiness('protocolo','setor');
        $thism->getPerms();
        parent::__construct('Pesquisa de sistemas');
        $this->SetClose($this->manager->GetActionURL('protocolo','main'));
        $this->eventHandler();
    }

    function CreateFields() {

        $descricao=new MTextField('desricao',60,58);
        $grid = $this->manager->GetUI()->GetGrid('gridVersaoSistemas');
        $grid->SetTitle('');
    }

    public function btnLista_Click() {
        global $MIOLO;

        $this->grid2->setTitle('Atualizar');

        $MIOLO->GetActionURL('main:versaosistemas:new');
    }

}
?>