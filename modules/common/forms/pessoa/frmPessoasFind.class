<?
MIOLO::Import('modules::common::controls::linkpessoa');

class frmPessoasFind extends Form
{
    var $pessoa;

    function frmPessoasFind()
    {
        global $MIOLO;
    
        $this->Form('Pessoas');
        $this->SetClose($MIOLO->GetActionURL('common','main'));
        $this->EventHandler();
    }

    function CreateFields()
    {
        global $MIOLO, $module, $page;

        $ui = $MIOLO->GetUI();
        $grid = $ui->GetGrid('common','gridPessoas');
        $grid->SetTitle(NULL);

        $btnFind = new ButtonFind('');
        $newLink = new LinkPessoa('lnkNew',FALSE);
        $newLink->setNew();
        $fields = array(
            array(
                new MTextField('nome','','Nome',45),
                $btnFind,
            ),
            $newLink,
            $grid
        );
        $this->SetFields($fields);
        $this->defaultButton = false;
    }

    function btnFind_click()
    {



    }

 }
?>
