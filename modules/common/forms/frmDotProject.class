<?php
class frmDotProject extends MForm
{
    var $objUsuario;

	function __construct()
    {
        global $MIOLO, $module, $page, $context, $self, $action;

        $id = $MIOLO->getLogin()->idkey;
        $this->objUsuario = $MIOLO->GetBusiness($module,'usuario', $id);
		parent::__construct('Gerenciamento de Projetos - DotProject UFJF');
	    $this->eventHandler();
	}

    function createFields()
	{
	   global $auth, $perms, $action;
       $fields = array(
           new MLabel('Use o link abaixo para abrir o DotProject em uma nova janela/aba.','blue',true),
           new MLink('dotproject','Link:','http://www.dotproject.ufjf.br/','http://www.dotproject.ufjf.br/','_dotproject'),
           new MSpacer('7px'),
       );
	   $this->SetFields($fields);
	   $this->defaultButton = false;
	}

}
?>