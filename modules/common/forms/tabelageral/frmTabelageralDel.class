<?php
class frmTabelageralDel extends MForm
{
	protected $obj;
    protected $objId;

    function __construct($obj)
    {
		$this->obj = $obj;
        parent::__construct('Excluir Tabelageral');
        $this->eventHandler();
    }

    function createFields()
    {
        global $MIOLO;

        $id = $this->obj->idTabelaGeral;
        $txt = $this->obj->tabela;

		$action_sim = $MIOLO->getActionURL('common','main:tabelageral',$id,array('form'=>'excluir','conf'=>'sim'));
		$action_nao = $MIOLO->getActionURL('common','main:tabelageral',$id);

		$conf = $this->getFormValue('conf');

		if ($conf == 'sim')
		{
            try
            {
    			$this->obj->delete();
	    		$MIOLO->Information('Tabelageral excluído com sucesso',$MIOLO->getActionURL('common','main:tabelageral:find'));
			}
			catch ( Exception $e )
			{
				$MIOLO->Error($e->getMessage,$MIOLO->GetActionURL('common','main:tabelageral',$this->id));
			}
					
	   }
	   else
	   {
		   $MIOLO->Question("Tem certeza que deseja excluir a Tabelageral ".$txt."?",$action_sim,$action_nao);
		}
    }
 }
?>