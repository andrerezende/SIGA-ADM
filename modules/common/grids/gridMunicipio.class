<?
class gridMunicipio extends MDataGrid
{
	function __construct()
	{

        $MIOLO = MIOLO::getInstance();

		$href = $MIOLO->getActionURL('common','main:municipio:main','#0#');

		$columns = array(
				new MDataGridColumn('idMunicipio','Código','center',false,'10%'),
                new MDataGridHyperlink('municipio','Municipio',$href,'90%',true,NULL,true),
        );

		$filter = MForm::GetFormValue('municipio');

        if ( is_null($filter) )
        {
            $filter = "A Definir";
        }

		$obj = $MIOLO->GetBusiness('common','municipio');

		$query = $obj->listByNome(strtoupper($filter));

		$hrefGrid = $MIOLO->GetActionURL('common','main:municipio:find');

		parent::__construct($query,$columns,$hrefGrid,15);

		$this->SetFilter(false);

		$this->SetIndex(1);

		$this->SetLinkType('hyperlink');
	}

	function GenerateFooter()
	{
		if ( ! $this->data )
		$footer[] = $this->GenerateEmptyMsg();
		$footer[] = new MSeparator();
		$footer[] = $this->GenerateControls();
		return $footer;
	}
}
?>