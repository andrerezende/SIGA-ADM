<?
class gridGrupoUsuarios extends MDataGrid
{
    function __construct($grupo)
    {
        $MIOLO = Miolo::getInstance();
        
        $columns = array(
            new MDataGridColumn('login','Login','left',false,'20%',true),
            new MDataGridColumn('nome','Nome','left',false,'80%',true),
        );

        $href = $MIOLO->getActionUrl('common','main:mad:grupo:main',$grupo->idGrupo,array("form"=>"usuarios"));
        

        $query = $grupo->ListUsuariosByIdGrupo($grupo->idGrupo);
        parent::__construct($query,$columns,$href,20);
        $this->setTitle("Usuários com acesso ao grupo");
    }
}
?>
