<?
class BusinessCommonVersaoSistemas extends Business implements IAccess
{
    public $idVersao;
    public $Versao;
    public $dataVersao;
    public $descricaoAlteracoes;

       public function _construct($data = NULL)
    {
        return parent::_construct($data);
    }

    function ListAll()
    {
        $criteria =  $this->getCriteria();
        return $criteria->retrieveAsQuery();
    }
    
    function GetVersao($Versao)
    {
            $sql = new sql('versao','cm_versao','(idversao = (
select max(idversao) from cm_versao))');
            $query = $this->Query($sql,$Versao);
            return $query->result;
            var_dump($query);exit;
    }
            
}
?>
