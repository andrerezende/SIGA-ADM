<?php
MIOLO::Import('modules::controle::controls::linkEmpresa');
class frmEmpresaFind extends MForm
{
    protected $empresa;
    
    function __construct()
    {
        global $MIOLO;
        $this->empresa = $MIOLO->getBusiness('controle','empresa');
        parent::__construct('Empresa');
        $this->SetClose($this->manager->GetActionURL('controle','main'));
        $this->eventHandler();
    }
    function CreateFields()
    {
        $grid = $this->manager->GetUI()->GetGrid('controle','gridEmpresa');
        $grid->SetTitle(NULL);
        $btnFind = new ButtonFind('btnBusca');
        $busca  = Form::GetFormValue('busca');
        $novo = new LinkEmpresa('lnkNovo',FALSE);
        $novo->setNew();
        $formLink->width = '30%';
        $formLink->float = 'right';
        $formLink->padding = '6px';
        $formLink->height = '114px';
        $fields = array(
            new Spacer('5px'),
	    $novo,
            $grid,
        );
        $this->SetFields($fields);
        $this->defaultButton = false;
    }
 }
?>