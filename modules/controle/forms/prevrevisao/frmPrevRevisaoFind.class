<?php
MIOLO::Import('modules::controle::controls::linkprevrevisao');
class frmPrevRevisaoFind extends MForm
{
    protected $prevRevisao;
    
    function __construct()
    {
        global $MIOLO;
        $this->prevRevisao = $MIOLO->getBusiness('controle','prevrevisao');
        parent::__construct('Previsão de Revisão');
        $this->SetClose($this->manager->GetActionURL('controle','main'));
        $this->eventHandler();
    }
    function CreateFields()
    {
//      grid
        $grid = $this->manager->GetUI()->GetGrid('controle','gridPrevRevisao');
        $grid->SetTitle(NULL);
        $btnFind = new ButtonFind('btnBusca');
        $busca  = Form::GetFormValue('busca');
        $novo = new LinkPrevRevisao('lnkNovo',FALSE);
        $novo->setNew();
        $formLink->width = '30%';
        $formLink->float = 'right';
        $formLink->padding = '6px';
        $formLink->height = '114px';
//      fields
        $fields = array(
            new Spacer('5px'),
	    $novo,
            $grid,
        );
        $this->SetFields($fields);
        $this->defaultButton = false;
    }
 }
?>